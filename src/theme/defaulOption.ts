import { ThemeOptions } from '@mui/material';

const defaultOption: ThemeOptions = {
  breakpoints: {
    values: {
      xs: 0,
      sm: 768,
      md: 992,
      lg: 1200,
      xl: 1920,
    },
  },
};

export default defaultOption;
